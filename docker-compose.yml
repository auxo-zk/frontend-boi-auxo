version: '3.8'

services:
    frontend-builder:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3111:3000'
        environment:
            NODE_ENV: production # Set your desired Node environment

